# Outset

This repo now implements the PC algorithm for arbitrary mixed data. Great job!

## Remaining todo's

1. The documentation in docs/ needs to be written/updated. Feel free to include a little synthetic example.
2. Make sure that precommit doesn't complain.
3. Let's deploy the docu to github pages after everything is done.
4. Let's lastly add some github actions to make sure that the state in which the repo will be after these changes doesn't deteriorate.
